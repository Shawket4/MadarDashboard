/* eslint-disable */
// @ts-nocheck

export interface ResolveBranchRequest {
  /** Device GPS latitude (WGS-84). */
  latitude: number;
  /** Device GPS longitude (WGS-84). */
  longitude: number;
  /** Organization to search within. */
  org_id: string;
}
