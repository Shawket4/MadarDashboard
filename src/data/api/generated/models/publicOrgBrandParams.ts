/* eslint-disable */
// @ts-nocheck

export type PublicOrgBrandParams = {
/**
 * The shop, when the page already knows which one it is.
 */
org_id?: string;
/**
 * The first label of the hostname, when it does not — `rue` for
 * `rue.madar-pos.cloud`.
 */
slug?: string;
};
