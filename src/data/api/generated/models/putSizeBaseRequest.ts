/* eslint-disable */
// @ts-nocheck

export interface PutSizeBaseRequest {
  /**
     * `null` detaches the size from its base (its base lines are removed).
     * @nullable
     */
  base_id?: string | null;
}
