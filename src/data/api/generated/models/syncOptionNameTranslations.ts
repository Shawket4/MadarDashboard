/* eslint-disable */
// @ts-nocheck

/**
 * `{locale: name}`, as the dashboard authored it (`{}` when untranslated):
 * a till shows the option in its own language. Additive.
 */
export type SyncOptionNameTranslations = { [key: string]: unknown };
