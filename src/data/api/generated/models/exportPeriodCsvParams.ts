/* eslint-disable */
// @ts-nocheck

export type ExportPeriodCsvParams = {
/**
 * `bank` (a transfer file: name, account, amount) · `wallet` (numbers
 * and amounts) · `cash`; omitted = everyone, every figure (PAY-8).
 */
method?: string;
};
