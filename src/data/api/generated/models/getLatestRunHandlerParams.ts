/* eslint-disable */
// @ts-nocheck

export type GetLatestRunHandlerParams = {
/**
 * When true, return the latest run regardless of status so the client
 * can show failed runs (error_message) instead of an empty state.
 */
any_status?: boolean;
};
